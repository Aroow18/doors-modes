game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(50, 50, 50)
game.Lighting.MainColorCorrection.Contrast = 0.4
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(0.7), {Contrast = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(0.5),{TintColor = Color3.fromRGB(255, 255, 255)})
TW:Play()
    local roast = Instance.new("Sound")
    roast.Parent = workspace
    roast.Name = "roast"
    roast.SoundId = "rbxassetid://9125936117"
    roast.Volume = 0.5
    roast.Pitch = 3
    roast:Play()

--====== Load spawner ======---

local spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/V2/Source.lua"))()

---====== Create entity ======---

local entity = spawner.Create({
	Entity = {
		Name = "Matcher",
		Asset = "https://raw.githubusercontent.com/Aroow18/wa/refs/heads/main/matcher.rbxm",
		HeightOffset = -1
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 0
		},
		Shatter = true,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 100,
		Values = {7.5, 8, 0.1, 1} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 145,
		Delay = 3,
		Reversed = false,
	},
	Rebounding = {
		Enabled = false,
		Type = "Blitz", -- "Blitz"
		Min = 1,
		Max = 5,
		Delay = 0
	},
	Damage = {
		Enabled = true,
		Range = 50,
		Amount = 100
	},
	Crucifixion = {
		Enabled = true,
		Range = 50,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding", -- "Curious"
		Hints = {"You have been killed by Matcher.", "You must hide, running isn't an option.", "Pay attention for cues that hint it's arrival.", "Pay attention to sounds too, it's sound is recognizable."},
		Cause = "Matcher"
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")
end)

entity:SetCallback("OnEnterRoom", function(room, firstTime)
	if firstTime == true then
		print("Entity has entered room: ".. room.Name.. " for the first time")
	else
		print("Entity has entered room: ".. room.Name.. " again")
	end
end)

entity:SetCallback("OnLookAt", function(lineOfSight)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound)
	if startOfRebound == true then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth)
	local player = game.Players.LocalPlayer
	local playerGui = player:WaitForChild("PlayerGui")
	local character = player.Character or player.CharacterAdded:Wait()
	local humanoid = character:WaitForChild("Humanoid")
	local camera = workspace.CurrentCamera

	if newHealth == 0 then
local Match = workspace.Matcher
Match:Destroy()

local s = Instance.new("Sound")
s.SoundId = "rbxassetid://138479878223247"
s.PlaybackSpeed = 0.8
s.Looped = false

Instance.new("PitchShiftSoundEffect", s)
Instance.new("DistortionSoundEffect", s)
Instance.new("EqualizerSoundEffect", s)

s.Parent = workspace
s:Play()

local player = game.Players.LocalPlayer
local GUI = Instance.new("ScreenGui")
GUI.Name = "WarningGui"
GUI.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
GUI.IgnoreGuiInset = true
GUI.Parent = player:WaitForChild("PlayerGui")

local Image = Instance.new("ImageLabel")
Image.BackgroundColor3 = Color3.fromRGB(20,5,5)
Image.BackgroundTransparency = 0
Image.Size = UDim2.new(1,0,1,0)
Image.Image = "rbxassetid://15815325811" 
Image.ImageTransparency = 1
Image.Parent = GUI
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")

-- Configuration
local assetId = "rbxassetid://115677645012229"     -- Your image ID
local targetSize = UDim2.new(0, 570, 0, 570) -- Final size
local tweenTime = 1.3                       -- 1 second size/move duration

-- Create GUI
local player = Players.LocalPlayer
local gui = Instance.new("ScreenGui")
gui.IgnoreGuiInset = true
gui.Parent = player:WaitForChild("PlayerGui")

-- Create Image Label (Starts slightly to the left)
local img = Instance.new("ImageLabel")
img.Size = UDim2.new(0, 0, 0, 0)
img.Position = UDim2.new(0.4, 0, 0.4, 0) -- 30% from left
img.AnchorPoint = Vector2.new(0.5, 0.5)
img.Image = assetId
img.BackgroundTransparency = 1
img.Parent = gui

-- Tween Size & Position (Moves to the right)
local sizeInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
local moveTween = TweenService:Create(img, sizeInfo, {
	Size = targetSize,
	Position = UDim2.new(0.5, 0, 0.5, 0) -- Moves to 70% right
})
moveTween:Play()

-- Snap Rotation Loop (Instant, not smooth)
task.spawn(function()
	while img.Parent do
Image.BackgroundColor3 = Color3.fromRGB(25,5,5)
		img.Rotation = -7
		task.wait(0.04)
Image.BackgroundColor3 = Color3.fromRGB(100,0,0)
		img.Rotation = 7
		task.wait(0.04)
	end
end)

-- Fade out after moving
moveTween.Completed:Wait()
local fadeTween = TweenService:Create(img, TweenInfo.new(tweenTime), {ImageTransparency = 1})
fadeTween:Play()

wait(2)
GUI:Destroy()
ImageLabel:Destroy()
background:Destroy()
s:Destroy()
		task.wait(0.7)
		camera.CameraType = Enum.CameraType.Custom
		camera.FieldOfView = startFov
	else
		print("Entity has damaged the player")
	end
end)

--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run()

roast:Destroy()
